services:
  app:
    build: .
    ports:
      - "4000:4000"
    env_file: .env
    # depends_on:
    #   - postgres    # uncomment if using db-pg or db-prisma
    #   - mongo       # uncomment if using db-mongoose

  # Uncomment the DB matching your installed module.

  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: myapp
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

  # mongo:
  #   image: mongo:7
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongodata:/data/db

# volumes:
#   pgdata:
#   mongodata:
